import 'package:flutter/material.dart';

class VerticalScrollListPage extends StatelessWidget {
  const VerticalScrollListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
