import 'package:flutter/material.dart';

class VsCodeTestPage extends StatelessWidget {
  const VsCodeTestPage ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("VsCodeTestPage"),),
    );
  }
}